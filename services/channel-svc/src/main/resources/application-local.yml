# local profile：本地开发/E2E（mock 渠道 adapter 经 @Profile("local") 装配）。
# svc_channel 首批表已落 Flyway：数据源不再排除，走 application.yml 默认值
# （本地 PG localhost:15432/ishome，docker compose 项目 ishome-dev；ISHOME_DB_* 可覆盖）。
