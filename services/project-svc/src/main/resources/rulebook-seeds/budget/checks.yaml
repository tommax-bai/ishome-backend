domain: budget
form: check
inherit: _common/checks.yaml
# check 是纪律的唯一存在形式：不进 calibration 状态机，正当性锚在 decided_by（裁决记录）。
# check 身上不携带内容数值；数值阈值只能经 max_from/threshold_from 引用 lkp-*（该参数照常过可核性门）。
items:
  - {id: cr-price-commitment, type: regex_deny, scope: [art-budget-chapter],
     pattern: "总价|全包价|这个价能做|包工包料|约为\\s*\\d+\\s*万", message: "本章禁精确总价与成交承诺",
     decided_by: "规范规则 5.15（用户裁决 2026-08-23）"}
  - {id: cr-budget-no-commerce, type: field_deny, scope: [art-budget-chapter],
     require: "本章不得出现任何商品挂载", message: "监督类产物植入商品",
     decided_by: "规范规则 9.2（监督/博弈类产物中立性禁令）"}
  # v2.4 改口径（裁决 2026-08-29，version 1 → 2）：原 require 写的是"越界即降档为仅出结构占比、
  # 不出金额"，规则 5.15 已把这条整句作废——过期的行情仍是当时的真实行情，标了取数时间业主自己会
  # 折算，抹掉金额反而少给他一个判断维度。**违规形态因此反了个向**：从"过期还出金额"变成
  # "出了金额却没标取数时间与来源"。判据 id 不改：名字说的是"陈旧单价"，纪律仍在管这件事，
  # 变的是怎么处置（对照"名字说假话就 retire"的 cr-draft-not-in-paid——那条是名字本身作废了）。
  - {id: cr-budget-stale-price, type: cross_field, scope: [art-budget-chapter], version: 2,
     require: "任一消费的 attr-price-* 若 effective_to 越界，金额照常出，但该页必须挂它的取数时间与来源标注（provenance.annotationRequired 已在求值线判定，页级标注比对门禁执行）",
     message: "过期单价出了金额却没标取数时间与来源",
     decided_by: "规范规则 5.15（v2.4 裁决 2026-08-29：过期照常出金额+标注取数时间，原「仅出结构占比」作废）"}
  - {id: cr-budget-range-width, type: cross_field, scope: [art-budget-chapter],
     require: "区间宽度须与该条 confidence 匹配（lkp-budget-confidence-width）", message: "低置信数据给了窄区间",
     decided_by: "规范规则 5.15（区间宽度随置信调整，用户裁决 2026-08-23）"}
  # scope 从卡片级上移到**册级**（图 v0.2 §4 三作用域第三档，位置=渲染前；2026-08-28 改）。依据三条：
  #   ① **形态判据**：本条是 presence_require——"产物里必须有 DISCLAIM_PRICE 锁定文案"，约束的是装配
  #      结果有没有这件东西，不是某张卡片怎么写。装配是页/册级的活（图 v0.2 §2：页面装配是唯一知道
  #      "页"的节点），判据该落在看得见整册产物的那一层。
  #   ② **规则 2.4**：DISCLAIM_PRICE 是 gen-locked（引用锁定文案 ID 直接渲染，零生成），**写作层本就
  #      不该产出它**。挂在卡片级等于要求写手写一句它没有权限写的话——判据挂错层，要么写手怎么写都
  #      过不了，要么（现状）根本没人执行。
  #   ③ **真跑实证**：自迭代回路首采 §五-3——r12 那张 verdict=ok 的造价卡片**没有挂 DISCLAIM_PRICE**，
  #      规则 5.15"必挂"在成文线上零拦截。挂错层就是零拦截的原因：单元级出口既看不见册，Card 模型
  #      也没有可挂免责文案的字段。册级（渲染前）是最后一个能停下来、也是唯一看得见整册的地方。
  # 保留 art-budget-chapter：作用域上移改的是"在哪一层判"，不改"判哪个产物"。
  # 只改 scope 与本注释；decided_by 不动——纪律本体未变，仍锚规则 5.15（挂错层是落位问题，不是裁决变更）。
  - {id: cr-budget-disclaimer, type: presence_require, scope: [册级, art-budget-chapter],
     require: "DISCLAIM_PRICE 锁定文案必挂（gen-locked，引用文案 ID）", message: "造价章缺 DISCLAIM_PRICE",
     decided_by: "规范规则 5.15"}
